[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mypackage"
version = "0.1.0"
description = "Simple demo Python package"
authors = [
  { name="Student", email="student@example.com" }
]
readme = "README.md"
requires-python = ">=3.8"
